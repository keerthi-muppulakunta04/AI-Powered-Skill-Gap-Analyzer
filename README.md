# 🌟 AI Skill Gap Analyzer

This project is an AI-powered tool that compares resumes and job descriptions to identify skill gaps.

### 🚀 How to Run
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the app:
   ```bash
 python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py

   


